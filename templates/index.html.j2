{% extends "layout.html.j2" %}

{% block content %}
   <h1>Home Page</h1>
   <ul class="poll-list">
      {% for poll in polls %}
         <li>
            <a class="poll-link" href="{{ url_for('poll', id = poll.id) }}">{{ poll.name }}</a>
         </li>
      {% endfor %}
   </ul>
{% endblock %}
